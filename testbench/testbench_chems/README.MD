# Ça marche sa mère let's goooo
1) télecharger le code
2) compiler test.c en utilisant la commande suivante :
gcc matrice.c myBmpGris.c gestionfichier.c calcul.c image.c test.c -lm -g
3) suivre les instructions
